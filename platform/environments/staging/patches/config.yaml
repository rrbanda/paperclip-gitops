apiVersion: apps/v1
kind: Deployment
metadata:
  name: paperclip
spec:
  template:
    spec:
      containers:
        - name: paperclip
          env:
            - name: PAPERCLIP_PUBLIC_URL
              value: "https://paperclip-paperclip-staging.apps.CLUSTER_DOMAIN"

apiVersion: apps/v1
kind: Deployment
metadata:
  name: paperclip
spec:
  template:
    spec:
      containers:
        - name: paperclip
          resources:
            requests:
              memory: "2Gi"
              cpu: "1"
            limits:
              memory: "16Gi"
              cpu: "4"

apiVersion: apps/v1
kind: Deployment
metadata:
  name: paperclip
spec:
  template:
    spec:
      containers:
        - name: paperclip
          resources:
            requests:
              memory: "4Gi"
              cpu: "2"
            limits:
              memory: "32Gi"
              cpu: "8"
